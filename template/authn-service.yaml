apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: authn-service
  namespace: foo
spec:
  hosts:
  - "*"
  ports:
  - number: 5000
    name: http
    protocol: HTTP
  resolution: STATIC
  endpoints:
  - address: {AUTHN-SERVICE-HOST}
  location: MESH_EXTERNAL